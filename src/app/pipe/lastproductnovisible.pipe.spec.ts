import { LastproductnovisiblePipe } from './lastproductnovisible.pipe';

describe('LastproductnovisiblePipe', () => {
  it('create an instance', () => {
    const pipe = new LastproductnovisiblePipe();
    expect(pipe).toBeTruthy();
  });
});
